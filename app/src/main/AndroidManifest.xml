<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.eeontheway.android.applocker">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.GET_TASKS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_lanucher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".main.StartupActivity"
            android:autoRemoveFromRecents="true"
            android:excludeFromRecents="true"
            android:theme="@style/Activity.NoTitleFullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".main.MainActivity" />
        <activity android:name=".applock.AppLockLogListActivity" />
        <activity android:name=".applock.AppLockSettingsActivity" />

        <service
            android:name=".applock.AppLockService"
            android:enabled="true"
            android:exported="true"
            android:priority="1000" />

        <activity
            android:name=".applock.AppLockPasswordVerifyActivity"
            android:launchMode="singleInstance"
            android:theme="@style/Activity.NoTitle" />
        <activity
            android:name=".applock.AppLockLogActivity"
            android:launchMode="singleInstance"
            android:theme="@style/Activity.NoTitle"></activity>

        <receiver
            android:name=".reciever.BootCompleteReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <activity android:name=".applock.AppLockPasswordSetActivity"></activity>
    </application>

</manifest>
